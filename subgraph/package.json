{
  "name": "rsvp-dapp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ ephcrat/rsvp-dapp",
    "create-local": "graph create --node http://localhost:8020/ ephcrat/rsvp-dapp",
    "remove-local": "graph remove --node http://localhost:8020/ ephcrat/rsvp-dapp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ephcrat/rsvp-dapp",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0",
    "@protofire/subgraph-toolkit": "^0.1.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
